# GEOSYNC India

Simple React/Vite project structure.

## Run

npm install
npm start

## Structure

HomePage contains the homepage Navbar, Body, and Footer. Other linked pages live in `src/pages`. JSX and CSS stay together for each part. Services contain API/database logic, data contains static data, and assets contain project files.
